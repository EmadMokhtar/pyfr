# Changelog

Generated by [Commitizen](https://commitizen-tools.github.io/commitizen/)
from Conventional Commit messages. Never edited by hand — a change here
comes from a commit message, and `.github/workflows/release.yml` rewrites
this file on every release.

Versions before v0.5.0 were never tagged: v0.5.0 is the first release, and
the entries below it are the history that produced it.

## v0.13.0 (2026-09-16)

### Feat

- harden the weekly template update and report the release url (#56)

## v0.12.0 (2026-09-15)

### Feat

- let a generated project update itself from the template (m8 pr 2) (#53)

## v0.11.0 (2026-09-15)

### Feat

- add pyfr-cli with pyfr update and update-check (m8 pr 1) (#52)

## v0.10.0 (2026-09-14)

### Feat

- prove a generated project passes its own gates and close m7 (m7 pr 5) (#50)

## v0.9.0 (2026-09-14)

### Feat

- give a generated project its own documentation site and split pyfr's docs (m7 pr 4) (#48)

## v0.8.0 (2026-09-13)

### Feat

- give a generated project its own github workflows and commitizen (m7 pr 3) (#44)

## v0.7.0 (2026-09-13)

### Feat

- add the backend prompts and prune the unchosen backends (m7 pr 2) (#37)
- move the reference service into the template body and add the golden diff (m7 pr 1) (#34)

## v0.6.0 (2026-09-12)

### Feat

- audit, scan, publish and update what ships (m6 supply chain) (#21)

## v0.5.0 (2026-09-11)

### Feat

- **docs**: warn on unreviewed pages and uncovered code changes
- **reference-service**: generate the configuration reference from the model
- **reference-service**: describe every setting on the model itself
- **reference-service**: add a settings walker for generated config docs
- **reference-service**: add the redis cache and the s3 receipt store (#19)
- **reference-service**: govern the api contract and add the outbound payment client (#18)
- **reference-service**: add opentelemetry observability, slos and grafana dashboards (#17)
- **reference-service**: add postgresql persistence and schema governance gates (#16)
- add the m0 walking skeleton reference service
- add the m0 walking skeleton reference service
- add service.name, service.version and deployment.environment to logs
- add orders api slice with separate schemas and mappers
- add correlation id and structured access log middleware
- map domain errors to rfc 9457 problem details
- add composition root, lifespan and health endpoints
- add an allowlist purity test for the m0 inner layers
- add in-memory order repository adapter
- add a static protocol conformance check to m0 task 7
- add place order and get order use cases
- add domain errors and order repository port
- add order aggregate with pydantic invariants
- add structlog configuration with standard library bridge
- add pydantic settings with fail-fast validation

### Fix

- **lychee**: anchor example.com pattern to prevent false exclusions
- **reference-service**: declare annotated-types and pydantic-core as direct deps
- **justfile**: render readable just --list descriptions (#13)
- **api**: carry status_code on request.validation_error and fix a typo
- **justfile**: fail just check loudly if pre-commit mutates the tree
- **compose**: drop the duplicated, drifting healthcheck block
- **justfile**: stop just dev double-logging every request
- **api**: document the real error responses in the OpenAPI schema
- **settings**: freeze LogSettings and OtelSettings
- **services**: make PlaceOrderCommand.lines immutable
- **compose**: raise the container kill deadline above the app's drain deadline
- **logging**: bridge uvicorn's own loggers into the structlog chain
- **health**: stop /readyz leaking exception detail
- **order**: make Order frozen to stop assignment corrupting it
- scope just check's pre-commit run to the project's own files
- stop just check from running an unlocked, mutating pre-commit
- make the commit-msg hook actually installable
- log the pydantic.ValidationError handler's 422 at warning
- reject mixed-currency order lines as 422, not an unhandled 500
- make Order.lines a tuple so the docstring's claim is actually true
- align error-handler log fields with the access log's naming
- reject an unknown log level at startup instead of crashing
- make APP_HTTP_PORT load-bearing
- keep the correlation id on a 500 response and its traceback log
- reject bad decimal input as 422, not an unhandled 500
- repair pre-commit config and pin it to the locked toolchain
- correct the expected total in the m0 compose smoke test
- move the m0 container images to debian trixie
- pin the m0 builder image to the uv major in use
- reconstruct access-log route templates positionally, not by text substitution
- reconstruct the route template positionally, not by text
- rebuild the m0 access-log route template from path params
- log <unmatched> instead of the raw path for 404s
- stop the m0 access log falling back to raw paths on 404
- assert the real invariant in the m0 correlation leak test
- map subclassed domain errors to their parent status
- modernise the 422 constant and honour subclasses in status_for
- place the async109 suppression on the parameter line in m0 task 9
- run readiness checks concurrently instead of sequentially
- run m0 readiness checks concurrently, not sequentially
- add include_external_packages to the m0 import-linter config
- share the fake order repository between m0 task 6 test files
- check domain-error http purity on instances, not classes
- align type-ignore style and strengthen order total property test
- strengthen the m0 order property test and unify the type-ignore pattern
- revert overly broad mypy config and apply targeted type ignore fix
- resolve five pre-flight conflicts in the m0 plan

### Refactor

- rename the import contract id to match the services layer
- **reference-service**: rename service layer to services
- **reference-service**: rename application layer to service, merge order use cases
- use pytest.MonkeyPatch directly in the session-scoped fixture
